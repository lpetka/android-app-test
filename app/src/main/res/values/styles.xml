<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
    </style>


    <style name="MyActionBar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="FloatingActionButton" parent="Widget.Design.FloatingActionButton">
        <item name="android:src">@drawable/ic_action_plus_white</item>
        <item name="android:tint">#efeded</item>
        <item name="rippleColor">#ac0a00</item>
        <item name="backgroundTint">#d71308</item>
    </style>

</resources>
